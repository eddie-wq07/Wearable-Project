package com.example.testwatch.sensors;

/** Constants for on-wrist vs off-wrist detection state (Android's native off-body sensor, not the
 *  Samsung SDK). */

public class OffBodyStatus {
    public static final int DEVICE_ON_BODY = 1;
    public static final int DEVICE_OFF_BODY = 0;
}
